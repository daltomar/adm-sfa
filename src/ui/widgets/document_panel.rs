use eframe::egui;

/// Drag-drop zone, thumbnail list, and label picker for a record's documents.
/// Stub — to be implemented once the DB query layer is ready.
#[allow(dead_code)]
pub fn show(ui: &mut egui::Ui, _record_type: &str, _record_id: i64) {
    ui.label("Documents: (not yet implemented)");
}
